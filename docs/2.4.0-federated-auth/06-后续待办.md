# 06 · 后续待办

本期不做。接口已按这些方向留空，避免再拆一次内核。

钉钉 HTTP 客户端内聚、删除 `internal/thirdparty/dingtalk` 已并入 **F1**，不再列在此。

| 项 | 说明 | 为何不跟 T16 绑 |
|----|------|-----------------|
| 通用 OIDC Provider | `[oidc.default] issuer / client_id / client_secret`，授权码 + 校验 ID Token（`iss`/`aud`/`exp`/`nonce`）。Keycloak / 云 IAM 只改配置。 | 真有 IdP 再开 F3；无需求则内核预留即可 |
| GitHub 等纯 OAuth | 无 ID Token，UserInfo 填 Subject；仍实现 `Provider`。 | 有通用 OIDC 后多半走 IdP 代理，Doc 侧可不做 |
| SAML | `Callback.Form` 已能接 `SAMLResponse`；需独立 ACS 与证书轮换。 | 另一套协议，工作量与钉钉不在同一量级 |
| 换绑 UI | 已登录用户绑定/解绑第二 IdP；禁止仅凭邮箱自动合并。 | 设置页产品，不是登录回调 |
| 联邦账号设本地密码 | 可选；设密前保持 `Login` 拒绝。 | 改密码流，与 IdP 对接无关 |
| T11 CSP | Round 6，建议 Vite 后再收紧。钉钉：`login.dingtalk.com`、`oapi.dingtalk.com`；企微：`open.weixin.qq.com`、`qyapi.weixin.qq.com`。 | 默认全关；跟联邦同批上 CSP 会先打断扫码 iframe |
| 企微独立扫码组件 | 仍走 `wework.Complete`，只改 `Start` 的 `Kind`。 | F2 网页授权已能登录；无 PC 扫码需求不做 |

不把 Access Token 当登录态；不把浏览器 Session 改成 JWT；不自建授权服务器（「用 Doc 登录别的站」）。
