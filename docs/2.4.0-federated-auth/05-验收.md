# 05 · 验收

未改代码前本表不勾。落实后按项勾。

## Web

- [ ] `/login`：未启用任何联邦 Provider 时只有账密（及注册/验证码原逻辑）。
- [ ] `[oauth.dingtalk] enable=true` 且密钥齐全：登录页出现钉钉入口；扫码/应用内登录后 Session 为**该员工对应 Member**，再扫另一员工是另一 `member_id`。
- [ ] 同一钉钉 userid 两次登录：identity 一行，不建第二人。
- [ ] `[oauth.wework] enable=true`：微信内打开授权 URL → 回调 → 建 Session。
- [ ] 伪造 `state` 或复用已消费 state → 403，不写 Session。
- [ ] `auto_create_member=false` 且无绑定 → 不建号、不登录。
- [ ] `members.status=1` 的绑定用户 → 拒绝登录。
- [ ] 本地密码、LDAP（若环境有）、`http_login_url` 与改前一致。
- [ ] 联邦账号用账密 POST `/login` → 失败（`auth_method=federated`）。
- [ ] `/dingtalk_login`、`/qrlogin/dingtalk` → **404**（已删除）。
- [ ] 代码与 go.mod 引用中无 `internal/thirdparty/dingtalk`（历史文档 `round-1-4` 除外）。
- [ ] 生产：`baseurl` 与钉钉/企微后台 Redirect URI 一致且 HTTPS。

## MCP

- [ ] 协议与工具不变。联邦登录用户可在 `/member/api-tokens` 签发 Token（权限随该 Member 角色）。
- [ ] 不改 Bearer 校验与缓存键。

## 命令

```bash
make test
# 或 just test
# Windows: powershell -File deployments/scripts/test.ps1
```

- [ ] 白名单包通过；新增 federated / identity 测试覆盖绑定与 state。

## 配置与文档

- [ ] `conf/app.conf.example` 无 `[dingtalk]`、无 `DOC_DINGTALK_READER`。
- [ ] `docs/router/routers-reference.md` 已换新路径。
- [ ] 上游 [upstream-mindoc-checklist.md §3.2](../upstream-mindoc-checklist.md#32-oauth2-登录重写) 改为指向本目录并注明「联邦登录落地」。
- [ ] CHANGELOG Unreleased 写明：删除旧钉钉路由、临时读者与 `internal/thirdparty/dingtalk`；需配新回调 URL。

## 升级注意

无历史外部登录绑定可迁。升级后若有人仍收藏旧 URL，会 404。钉钉/企微应用后台必须改 Redirect URI 后再开 `enable`。
