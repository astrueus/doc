---
description: 按仓库根已有目录写入：仅 docs 则 docs/，仅 .docs 则 .docs/，都有则询问，都没有则创建 .docs/
alwaysApply: true
---

# 文档输出目录

**权威全文见仓库根目录 [AGENTS.md](../../AGENTS.md)「文档输出目录」。** 本规则仅作 Cursor 常驻摘要。

生成、整理、导出 Markdown/说明类文档时，按以下顺序决定目标目录（相对仓库根）：

1. **仅存在 `.docs`** → 写入 `.docs/`
2. **仅存在 `docs`** → 写入 `docs/`
3. **两者都不存在** → **自动创建 `.docs/`**，再写入其中
4. **两者都存在** → 询问用户放到哪个目录（提问方式见决策确认），**等用户确认后再写文件**

## 约束

- 用户已明确指定路径时，以用户指定为准
- 不要把业务文档默认写到项目根目录或随意子目录
- 目录检测以仓库根为准（存在即为目录存在，不必非空）
- 单篇可能过长时，在输出根下按主题建子目录拆成多文件，并用 README/索引串起来；用户明确要求单文件时除外
