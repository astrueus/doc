---
description: 关键决策先确认；小改动直接执行；对外契约与鉴权不可被「修 bug」跳过
alwaysApply: true
---

# 决策确认（AskQuestion）

本规则为**仓库自包含**约定，不依赖协作者是否配置了用户级全局规则。全文以本文件为准。

目标：在影响系统行为、架构、兼容性或生产风险的决策前，先通过 AskQuestion 让用户确认。  
不要因为普通技术选择频繁打断用户。

### 冲突时以谁为准

「必须询问」与下文「直接执行」同时命中时：

- **下列仍必须询问**（即使看起来像 bug 修复或小改动）：对外契约、权限与鉴权、删除或迁移数据、未明确要求的 `git commit` / `push` / `amend`、生产或环境配置、不可逆命令
- 其余同时命中 → 以「直接执行」为准，不提问

---

## 必须使用 AskQuestion

以下情况**必须先询问**，用户确认前：不修改相关代码、不提交、不执行危险操作。

### 1. 存在业务影响

- 修改对外接口或协议的结构 / 字段含义（HTTP、RPC、MCP 等）
- 改变用户可见行为或交互流程
- 删除 / 迁移数据
- 修改权限 / 鉴权 / 登录态

### 2. 存在兼容性选择

- 是否兼容旧客户端
- 是否保留旧 API / 旧字段 / 旧协议行为
- 是否迁移历史数据
- 是否破坏已有调用方或上游兼容

### 3. 存在架构选择

- 新增服务 vs 扩展现有服务
- 同步 vs 异步处理
- 存储、缓存、中间件等方案取舍
- 模块拆分或跨层大重构路径

### 4. 存在操作风险

- `git commit` / `git push` / `amend`（用户未明确要求提交或推送时）
- 删除文件或大批量清理
- 修改生产 / 环境相关配置
- 新增或改动数据库迁移
- 执行不可逆命令（重置、force、删库等）

### 5. 用户明确要求确认

- 「有疑问问我」「不要自行决定」「方案先确认」等类似表述
- 文档 / 执行计划与现状不一致时的取舍

### 6. 文档落盘冲突

- 仓库根同时存在 `.docs` 与 `docs`，且用户未指定输出目录时（见 `AGENTS.md`「文档输出目录」）

---

## 不需要 AskQuestion（直接执行）

以下情况**不要询问**，按本仓库已有规范直接做：

- 需求已明确的小范围代码修改
- bug 修复（行为目标清晰，且不改对外契约、鉴权、数据形态）
- 无业务影响的性能优化
- 添加 / 补全测试
- 增加日志或调试信息（不含改对外协议）
- 代码格式、命名微调、注释补充
- 按本仓库已有目录与分层补全同类代码（不要套用其他语言/框架的分层名称）
- 仅一种合理实现、且不影响兼容与数据的技术细节

---

## 默认原则

- 不确定但影响小 → 采用当前项目已有模式继续，不提问
- 不确定且可能导致返工 / 线上风险 → 必须 AskQuestion
- 不要把「已有明确唯一答案」的问题做成 AskQuestion

---

## 提问方式

1. **优先**用 `AskQuestion`（可点选卡片）
2. `AskQuestion` 不可用时，**先检查**当前环境是否有同类工具（可列出固定选项、让用户点选或回复）。有则用同类工具，规则与下款相同：每题 2～5 个选项、标推荐、写清差异
3. 同类工具也没有时，再用文本列出问题，**等用户确认后再执行**

### 选项怎么写

- **问题用序号**：`问题 1`、`问题 2`……（一次可以问多题）
- **选项用字母**：每题下用 `A.` `B.` `C.`，不要用 1/2/3 当选项
- 每题 2～5 个选项；标出**推荐项**
- 每个选项写完整：要做什么、和其它选项的差异、利弊/影响，让人不用猜
- 涉及代码或路径时，**适当附一点具体内容**（关键片段、文件路径、签名），不要只写「方案 A / 方案 B」
- 代码示例保持简短，服务理解即可，不要整文件粘贴
- 开放输入可另说明，或提供「其他（请直接描述）」
- 题目是 2～3 个方案里选、且对比还不清楚时，**必须提供一项**：不太确定，先给出具体代码或示例对照，看完再决定。选了该项后：先给出对照（差异、利弊、短代码/路径），**等用户看完并选定方案后再改代码**，不要直接动手

`AskQuestion` 可用时，同样遵守上款（含「先对照再决定」选项；文案可写成「不太确定，先对照代码/示例再决定」）。

### 文本提问模板（AskQuestion 与同类工具都不可用时）

```text
问题 1：xxx
为何要问：xxx（不确认会怎样）

选项：
A. xxx（推荐）
   说明：准备怎么做、和现状的差异
   影响：…
   示例：
   （可选，少量代码或路径）
B. xxx
   说明：…
   影响：…
   示例：…
C. 不太确定，先给出具体代码 / 示例对照后再决定
   说明：先不改仓库。先把各方案的关键差异写成对照（可附短代码或路径），你看完再选方案。

问题 2：xxx
为何要问：xxx

选项：
A. xxx（推荐）
   说明：…
   影响：…
B. xxx
   说明：…
   影响：…
C. 不太确定，先给出具体代码 / 示例对照后再决定
   说明：先不改仓库，先对照再选。

请按题号回复选项字母（如：问题 1 选 A，问题 2 选 C），或直接写完整说明。
确认前不修改相关代码、不提交。
```
